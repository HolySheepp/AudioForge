appId: com.francishuang.audioforge
productName: AudioForge
directories:
  buildResources: build
  output: release
files:
  - out/**
extraResources:
  - from: bin
    to: bin
    filter:
      - "**/*"
win:
  target:
    - target: nsis
      arch:
        - x64
    - target: portable
      arch:
        - x64
    - target: zip
      arch:
        - x64
  icon: build/icon.png
portable:
  artifactName: AudioForge-${version}-portable.exe
nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  shortcutName: AudioForge
electronDownload:
  cache: .electron-cache
npmRebuild: false
